$(function() {
  // DatePicker
	$("#datepicker").datepicker({
		autoclose: true,
		todayHighlight: true,
		format: 'dd/mm/yyyy',
		pickerPosition: "bottom"
	}); // .datepicker('update', new Date())
});